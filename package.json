{
  "name": "dns-prevent-error-plugin",
  "version": "1.0.0",
  "description": "a plugin of dns prevent error",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node --inspect-brk build.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.20.1"
  },
  "dependencies": {
    "html-webpack-plugin": "^5.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
